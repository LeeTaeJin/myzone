
@save_result=<%=@save_result%><br>
@date=<%=@date%><br>
@start_time=<%=@start_time%><br>
@start_time=<%=@finish_time%><br>
(Time.zone.now <= @start_time) and (@start_time < @finish_time)= <%=(Time.zone.now <= @start_time) and (@start_time < @finish_time)%><br>
@all_reservation.nil? <%=@all_reservation.nil?%><br>
@all_reservation==nil <%=if @all_reservation==nil 
true
end%><br>
<!--@all_reservation.empty? %=@all_reservation.empty?%><br>-->
<!--@all_reservation.blank? %=@all_reservation.blank?%><br>-->
@all_reservation.count <%=@all_reservation.count%><br>
@all_reservation.exists? <%=@all_reservation.exists?%><br>
@all_reservation.any? <%=@all_reservation.any?%><br>

<% @all_reservation.each do |r| #부들부들 %>
    <%=r.start_time%>
    <%=r.finish_time%><br>
<%end%>

    
<%
@all_reservation.each do |r| #부들부들

  if ((@start_time.between?(r.start_time, r.finish_time)) or 
      (@finish_time.between?(r.start_time, r.finish_time)) or
      ((@start_time < r.start_time) and (r.finish_time < @finish_time)))

    @save_result = "3번리죨트"    #중복일 때 false
    
  else #안겹치면 저장
    
     @save_result = "4번리죨트"
  end
end  %>
<%=@save_result%>